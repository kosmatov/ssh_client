language: ruby
env:
  - SSHUSER=travis
rvm:
  - 2.1.2
  - 2.3.0
before_install: 
  - sudo apt-get -qq update
  - sudo apt-get install sshpass
  - cat ~/.ssh/id_rsa.pub >> ~/.ssh/authorized_keys
  - gem install bundler
